
# Selection Sort
This folder covers the Selection Sort algorithm: a basic sorting method that finds the minimum element and swaps it into place.

**Complexity:** O(n²) time, O(1) space.
Files: Implementation in Python 
